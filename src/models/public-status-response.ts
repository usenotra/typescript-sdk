/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 08043f4450fb
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export const Status = {
  Ok: "ok",
} as const;
export type Status = ClosedEnum<typeof Status>;

export const Service = {
  NotraAPI: "Notra API",
} as const;
export type Service = ClosedEnum<typeof Service>;

export const TypeEnum = {
  Bearer: "bearer",
} as const;
export type TypeEnum = ClosedEnum<typeof TypeEnum>;

export type Authentication = {
  type: TypeEnum;
  resourceMetadata: string;
  guide: string;
};

export type PublicStatusResponse = {
  status: Status;
  service: Service;
  version: string;
  public: true;
  authentication: Authentication;
};

/** @internal */
export const Status$inboundSchema: z.ZodMiniEnum<typeof Status> = z.enum(
  Status,
);

/** @internal */
export const Service$inboundSchema: z.ZodMiniEnum<typeof Service> = z.enum(
  Service,
);

/** @internal */
export const TypeEnum$inboundSchema: z.ZodMiniEnum<typeof TypeEnum> = z.enum(
  TypeEnum,
);

/** @internal */
export const Authentication$inboundSchema: z.ZodMiniType<
  Authentication,
  unknown
> = z.pipe(
  z.object({
    type: TypeEnum$inboundSchema,
    resource_metadata: types.string(),
    guide: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "resource_metadata": "resourceMetadata",
    });
  }),
);

export function authenticationFromJSON(
  jsonString: string,
): SafeParseResult<Authentication, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Authentication$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Authentication' from JSON`,
  );
}

/** @internal */
export const PublicStatusResponse$inboundSchema: z.ZodMiniType<
  PublicStatusResponse,
  unknown
> = z.object({
  status: Status$inboundSchema,
  service: Service$inboundSchema,
  version: types.string(),
  public: types.literal(true),
  authentication: z.lazy(() => Authentication$inboundSchema),
});

export function publicStatusResponseFromJSON(
  jsonString: string,
): SafeParseResult<PublicStatusResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicStatusResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicStatusResponse' from JSON`,
  );
}
