/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 06f237b15c52
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type Skill = {
  id: string;
  name: string;
  description: string;
  isSystem: boolean;
  updatedAt: string;
  content: string;
  createdAt: string;
};

/** @internal */
export const Skill$inboundSchema: z.ZodMiniType<Skill, unknown> = z.object({
  id: types.string(),
  name: types.string(),
  description: types.string(),
  isSystem: types.boolean(),
  updatedAt: types.string(),
  content: types.string(),
  createdAt: types.string(),
});

export function skillFromJSON(
  jsonString: string,
): SafeParseResult<Skill, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Skill$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Skill' from JSON`,
  );
}
